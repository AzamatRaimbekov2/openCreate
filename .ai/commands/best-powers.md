---
description: Bootstrap this folder with the full Ruflo toolkit (skills, agents, hooks, MCP, .ai layout)
---

Invoke the `best-powers` skill for the current folder (or the path given in $ARGUMENTS).
If the skill is unavailable, run: `bash ~/.claude/skills/best-powers/scripts/setup.sh $ARGUMENTS`
