# openCreate — Design System v4 ("Bioluminescent Terminal, frosted")

> Canonical design source of truth for `apps/web`. Every screen, component, and token
> decision starts here. v3 created 2026-07-07 from the owner-chosen Midjourney-style
> reference (`docs/frontend/style-reference-v3.md` — its §Adaptations are binding),
> superseding v2 "Light Editorial". **v4 (2026-07-09)** keeps all of v3 except the
> depth model: cards and sheets became iOS-18-style frosted glass (§3.5) at the
> owner's request. The no-gradient law, the surface ladder, the mono typography and
> the specimen-pill triad are unchanged. Keep in sync with
> `apps/web/src/shared/config/theme.css` and `apps/web/src/shared/ui/`.

## 1. Intent & identity

openCreate is an honest, cheaper AI image/video generation product. The UI reads like
a **bioluminescent research terminal**: a near-black cosmic void, whisper-weight
JetBrains Mono everywhere, elevation by surface color steps, and a closed triad of
translucent specimen pills — never a dashboard template, never a light theme.

- Direction: **"Bioluminescent Terminal"** — flat `#06051d` void, mono typography at
  weight 400, steel-navy surfaces, portal-blue prose links, three glowing pill tints.
- Audience: creators comparing generation prices; EN + RU locales, desktop-first,
  fully responsive (390px must wrap cleanly).
- Voice: honest copy — short verbs, sentence case, no exclamation marks, no blame.
  All strings via i18next keys present in BOTH `en.json` and `ru.json`. The four
  approved claims ($0.01 images / $0.35 5s video / credits never expire / no
  subscription) keep their exact meaning everywhere.
- **NO GRADIENTS — hard owner rule.** No background gradients, no gradient shimmer,
  no gradient fills in art or chrome. Flat colors only, everywhere. Grep every diff
  for `gradient` before committing. **This rule survives v4 untouched.**
- Depth (**v4, 2026-07-09**): **frosted glass.** Cards and sheets are translucent,
  backdrop-blurred and lifted with `shadow-glass`. This SUPERSEDES the v3 rule
  ("depth = surface color steps, the only shadow is `shadow-pill`"), which the owner
  retired when asking for iOS-18-style glass cards. Surface color steps still order
  the ladder (§2); glass is how a card sits ON it. See §3.5.

## 2. Color tokens

Defined once in `apps/web/src/shared/config/theme.css` via Tailwind v4 `@theme`.
Use utility classes (`bg-void`, `text-mist`, `bg-specimen-green/20`, …) — never raw
hex in components (single exception: `specimenTileArt.tsx` art data, §5; the
`AsciiSphere` canvas resolves the mist token via `getComputedStyle` with the
token-mirroring fallback — canvas `fillStyle` cannot consume `var()`).

### Surface ladder (elevation = color step)

| Token | Value | Step | Use when | Avoid when |
|---|---|---|---|---|
| `--color-void` | `#06051d` | 0 — the page | html/body, standalone screens, prose background (body text sits ON the void, not in cards) | Never pure `#000`; never a gradient |
| `--color-abyss` | `#0f1c36` | −1 — recessed | Media wells (generated images/video plates), the login manifesto panel, sunken panels | General cards (that is steel) |
| `--color-steel` | `#1d293d` | +1 — working surface | Sticky nav bar, cards, inputs/textareas/selects, modal sheets, calm banner blocks | Whole-page fills |
| `--color-ridge` | `#314062` | +2 — elevated/hover | Hovered rows/triggers, menu panels, active LangSwitch segment, Progress track | Resting card fills |

### Text & prose accent

| Token | Value | Role |
|---|---|---|
| (white) | `#ffffff` | Headings, numerals, model names, active nav — presence color |
| `--color-mist` | `#cad5e2` | Body text |
| `--color-mist-dim` | `#90a1b9` | Secondary text, captions, labels, inactive nav |
| `--color-portal` | `#63b3ed` | Links, the wordmark "·", decorative ordinals — **the only chromatic accent in prose**; also the focus-ring color |

### Specimen pill triad (closed system — max three button tints)

Pills are ALWAYS translucent: `bg-specimen-*/20` (hover `/35`) + `border-white/10` +
bright matching text + `shadow-pill`. **No solid opaque fills.**

| Tint | Base (bg at /20) | Text | Icon/status glow | Semantics |
|---|---|---|---|---|
| Green | `--color-specimen-green` `#004f3b` | `--color-glow-green` `#00bc7d` | `#00bc7d` | Create/submit/positive: «Начать создавать», Generate, Sign up, go-home, empty-state CTAs |
| Amber | `--color-specimen-amber` `#733e0a` | `--color-lumen-amber` `#fefce8` | `--color-glow-amber` `#f0b100` | Explore/browse/secondary: ghost buttons, Pricing, model-picker & toggle selection, credits chip |
| Red | `--color-specimen-red` `#8b0836` | `--color-lumen-red` `#fff1f2` | `--color-glow-red` `#ff2056` | Auth-exit + destructive: Sign in/Log in (reference taxonomy), Delete; failure status |

### Status mapping (same closed triad)

| Status | Color |
|---|---|
| processing | amber — `text-glow-amber` percent, amber selection family |
| succeeded / positive amounts / refunds | green — `text-glow-green` |
| failed / negative amounts / validation errors | red — `text-glow-red` text, `border-glow-red` rules/wells |

Hairlines and washes are `white/N` opacity modifiers — `border-white/10` (the standard
hairline), `border-white/15`–`/25` (interactive borders), `bg-white/5` (chip wash).
Do not add gray tokens. Decorative glyphs (bolts, play marks, chevrons) are inline SVG
in `currentColor` or plain text characters — **never OS color emoji** (they paint
their own palette and break the closed triad).

### Contrast notes (against void `#06051d` / steel `#1d293d`)

- white ~19:1, mist ~14:1, mist-dim ~8:1 — all text sizes AA+.
- portal ~9:1 — safe at all sizes (links, captions).
- glow-green ~8:1, glow-amber ~11:1 — safe at caption size.
- glow-red ~5.5:1 — safe for small status text; prefer ≥12px.
- lumen-amber / lumen-red ≈ near-white — safe everywhere (pill text).

## 3. Typography

Two self-hosted static families (imported in `main.tsx` via @fontsource, declared as
`@theme` font tokens; only 400 + 500 weights ship — heavier files would be dead bytes):

| Token | Family | Role |
|---|---|---|
| `--font-mono` | **JetBrains Mono** (latin + cyrillic, 400/500), fallback ui-monospace | **THE typeface** — body default, headings, nav, buttons, links, numerals, captions |
| `--font-sans` | **DM Sans** (latin only, 400/500), fallback system-ui | Sparing secondary body prose only; RU in sans contexts falls back to system sans (no Cyrillic subset exists) |

### Type rules (binding)

- **Weight ceiling: 500.** Nothing renders `font-semibold` or bolder — verify with
  `grep -r 'font-(semibold|bold)'`. Headings are `font-normal` (400): whisper-weight
  display is the signature.
- **Headings: 30px, weight 400, no uppercase transform.** `text-3xl font-normal
  text-white` is the page/section heading; no `md:` size escalation — hierarchy comes
  from white-vs-mist color and spacing. Sub-headings may drop to `text-2xl`/`text-xl`
  at the same weight.
- Body 16px/1.5 `text-mist`; captions/labels `text-xs`/`text-sm` `text-mist-dim`,
  always lowercase (v2's uppercase-tracked micro-labels are retired).
- Large display (hero) stays weight 400: `clamp(1.875rem,5vw,3.75rem)` — mono runs
  wide, so the scale tops out lower than a serif would.
- Numerals (prices, credits, percents, ordinals) are mono weight 400; white for
  neutral, triad glow only when the number IS a status, portal for decorative
  sequence marks.
- `font-medium` (500) is reserved for interactive labels (buttons, links, nav
  wordmark, model names).

## 3.5 Glass surfaces (v4 — the depth model)

Authored ONCE in `apps/web/src/shared/ui/surfaces.ts` and normally consumed through
the `Card` primitive (`Modal`'s sheet reads the same constants). **Never hand-write a
frosted class string.** Before v4 the recipe had been copy-pasted into three
components and the third copy (the composer capsule) had already drifted — a fainter
fill, a `bg-ridge` baseline, a bespoke shadow. That drift is the whole argument for a
single source.

| Surface | Constant | Use when | Never |
|---|---|---|---|
| **glass** | `GLASS_SURFACE` | The default card, at rest. Panels and modal sheets. | Popup/menu panels; anything that must stay legible over moving media |
| **glass, floating** | `GLASS_FLOATING` | The same material with a longer shadow throw: chrome hovering OVER scrolling content (the composer capsule) | A resting panel — the lift would read as a bug |
| **steel** | `STEEL_SURFACE` | Opaque working surface: text dialogs, dropdown panels, inputs | As a generic card (that is glass now) |
| **well** | `WELL_SURFACE` | Recessed plate the content sits INSIDE: media wells, the shot rail | Anything that should look raised |

There are exactly **two elevations**, not a shadow per call site: Tailwind resolves
competing shadow utilities by stylesheet order, not by class order, so "`GLASS_SURFACE`
plus my own shadow" is not something a consumer can express. Need a new elevation? Add
a named one here. `Select`'s trigger keeps its own translucent fill — a trigger is a
control, not a surface, and its popup PANEL is deliberately opaque.

Three properties of the glass recipe are load-bearing, and each exists for a reason:

1. **`bg-steel` is the baseline, not a fallback afterthought.** Every frosted utility
   is layered behind `supports-[backdrop-filter]:`. A browser without
   `backdrop-filter` therefore renders an opaque steel card — a translucent card over
   unblurred content is unreadable, not stylish.
2. **`backdrop-blur` only does visible work where there is TEXTURE behind the card.**
   Blurring a uniform fill returns the same fill, so over the flat `--color-void` the
   blur is a mathematical no-op. Glass reads over media (previews, thumbnails, the
   gallery grid) and over the dimmed page under a modal; elsewhere the translucent
   wash, the hairline and `shadow-glass` carry the look. Do not "fix" this with a
   background gradient — see the no-gradient rule.
3. **The specular edge is a brighter TOP BORDER** (`border-t-white/25`), never a
   gradient sheen. This is how the iOS-glass highlight is reproduced under the
   no-gradient law.

Media is still the hero (owner's standing feedback): a frosted card wrapping a
photograph adds chrome and buries it. Media plates take `surface="well"` +
`padding="none"` and let the image be the brightest thing on screen.

## 4. Structure, spacing, radius, motion

- **Surface elevation table (§2) is the layout law**: prose on the void, cards and
  inputs on steel, hovers/menus on ridge, media on abyss. The sticky steel nav needs
  no border — the color step separates it.
- **Radii: three.** Pills = `rounded-full` (buttons, chips, toggles, LangSwitch);
  **cards/modals = `rounded-2xl` (16px, v4 — the glass silhouette)**; inputs/media
  = `rounded-lg` (8px). Square ends for rules/progress meters.
- **Spacing**: 4px scale. `gap-1` inside controls, `gap-4` between fields, `p-6`–`p-8`
  framed blocks; landing sections keep ≥96px vertical rhythm on desktop (`md:gap-28`).
  Landing/prose column stays narrow; app screens (create/library) use the wider grid.
- **Shadows (v4)**: `shadow-pill` on specimen pills; `shadow-glass` on glass cards and
  sheets (it ships inside `GLASS_SURFACE`). Popup/menu panels: still none — they are
  opaque steel by design. Never author a bespoke shadow.
- **Motion**: opacity/translate/color only, 150–250ms (`duration-200` default).
  Hover = one surface step up (`hover:bg-ridge`) or one tint step (`/20 → /35`).
  `animate-skeleton` (stepped background-color walk abyss→steel→ridge→steel,
  `steps(1,end)`) is the loading pulse — a stepped SOLID pulse, never a gradient
  shimmer. Media plates may lift ≤2px on hover, `motion-safe` only.

## 5. Showcase art direction (`SpecimenTile`) + hero visual (`AsciiSphere`)

Stage 2 (2026-07-07) REPLACED the v2 editorial posters (`ShowcasePoster` +
`showcasePosterArt` are deleted): the showcase is now eight blue-violet DUOTONE
SVG "specimens" — `eye / brain / hand / arch / moon / koi / cell / orbit`
(the reference symbolism family + three same-language lab plates) — drawn with
FLAT fills, hairline strokes and SVG `<pattern>` textures ONLY (dots, 45°
hatch, scanlines; no gradients, no filters, no text). Square tiles in a 4-col
grid (2-col mobile), 8px gap + radius, `border-white/10` fog border. Chrome is
minimal: NO per-tile captions — ONE small mono caption under the grid carries
the honest "sample style" chip (`landing.showcase.sampleLabel`) and names the
REAL catalog models (`landing.showcase.caption`); exactly one tile (the moon)
is video-marked. The duotone (`SPECIMEN_GROUND #161233`, `SPECIMEN_INK
#8fa3f2` in `specimenTileArt.tsx`) is the only sanctioned raw-hex art site.

The hero visual also landed: `AsciiSphere` — a dependency-free animated ASCII
ellipsoid canvas (2d context, no WebGL) in the mist token at low opacity,
~30fps frame-capped rAF with full cleanup, `prefers-reduced-motion` → one
static frame, aria-hidden, transparent background.

## 6. Component inventory (`apps/web/src/shared/ui/`)

Import from `'shared/ui'` only (public API via `index.ts`). Reuse these before creating
anything new; new shared components must be added to this table in the same task.

| Component | Variants / props | v3 treatment & states |
|---|---|---|
| `Button` | `variant: primary \| ghost \| danger`, `size: sm \| md \| lg`, `isLoading` | Specimen pills: primary = green tint, ghost = amber tint, danger = red tint — always `bg-specimen-*/20 border-white/10` + bright text + `shadow-pill`, hover `/35`. Focus = portal ring; disabled 50%; loading spinner + `aria-busy`. `sm` (32px, `text-xs`, v3.1 compact scale) is for dense pointer-first tool chrome (editor toolbars) only — `md` stays the floor for page CTAs |
| `Input` | `label`, `labelHidden?`, `error`; native props incl. `ref` (RHF-ready) | Steel filled field: `bg-steel rounded-lg border-white/10`, mono caption label (`text-xs text-mist-dim`); focus = `border-portal`; error = `border-glow-red` + `role="alert"` glow-red message. **`labelHidden` (2026-08-20)** renders the label `sr-only` — ONLY for a dense data grid whose `<th scope="col">` already names the field (the Shorts variants table): the label still exists and still names the control, it is just not painted in forty cells, and a screen reader is not made to hear it forty times. It is NOT a way to ship a placeholder-only field. `Select` carries the same prop for the same reason and only that reason |
| `Skeleton` | `className` for shape | `animate-skeleton bg-steel rounded-lg` — stepped solid pulse through the surface ladder (no gradient shimmer) |
| `Card` **(v4)** | `surface: glass \| steel \| well`, `title?`, `action?`, `padding: none \| md \| lg`, `className` (layout only) | THE panel primitive — replaces every hand-rolled `rounded-lg border border-white/10 p-4`. `rounded-2xl`, surface from `surfaces.ts`. With `title` it renders the heading and becomes a `<section>` landmark named by it (`aria-labelledby`), so an editor screen is navigable by region. `padding="none"` for edge-to-edge media. `className` is for grid spans / sticky positioning — NEVER for surface styling |
| `Modal` | `isOpen`, `onClose`, `title`, `role: dialog \| alertdialog`, `size: md \| lg \| full`, `surface: steel \| glass`, `hideHeader` | `rounded-2xl` sheet over `bg-void/70`; **`full` (2026-08-02) is the VIEWER size** — `h-[92dvh] max-w-[96rem]`, and it is the only size that sets a HEIGHT: a two-pane child (media stage + information column) needs a definite height to divide, or the stage collapses to its content and the column's scroller never engages; `steel` = opaque (text dialogs), `glass` = frosted (media detail, where the content is the hero). Both surfaces come from `surfaces.ts`. Mono 400 title, white/10 hairline close circle (hover → ridge); portal, Escape + overlay close, scroll lock, dependency-free focus TRAP (Tab/Shift+Tab wrap inside), focus restore to the trigger. **THE CALLER OWNS THE SCROLLER (law, learned twice — a50f5a7 TemplateDetailModal, then StyleEditor at 1336×728):** the panel is a `max-h-[92dvh]` flex column, and a flex child defaults to `min-height:auto`, so ANY body that can exceed the viewport must be wrapped in `flex min-h-0 … overflow-y-auto` or it paints past the panel bottom, the wheel scrolls the PAGE behind the overlay, and the footer controls become unreachable by mouse. `min-h-0` is the half that does the work. A read-mostly sheet may keep its CTA inside that scroller; a tall FORM should pin its actions in a `shrink-0` footer below it so Save cannot be pushed out of reach |
| `EmptyState` | `icon?`, `title`, `description?`, `action?` | White/10 hairline `rounded-lg` frame on the void, mono 400 30px white title |
| `ErrorState` | `message`, `onRetry?` | Calm hairline frame + amber ghost retry, `role="alert"` — never red-primary |
| `EnhanceButton` **(2026-07-21)** | `value`, `onEnhanced`, `className?` | The prompt-enhancer affordance docked INSIDE a prompt field: a size-8 sparkle icon (stroke `currentColor` on the closed icon language — never an emoji). Click → `POST /api/prompt/enhance` (mode `enhance`) → REPLACES the field with the result + a quiet neutral **Undo** chip restoring the exact draft; pending swaps sparkle↔spinner in the same box (no shift, `aria-busy`); a failure is a calm AMBER `role="status"` chip floating above the icon (`provider_error`→"unavailable", `rate_limited`, else generic — never red panic, the storyboard-not-configured voice); an occasional idle NUDGE `role="tooltip"` appears at most once per session (shared `useEnhanceNudge`), never after dismiss, `motion-safe:` `@starting-style` enter, never steals focus. The ONE kit component with behaviour (drives the `shared/model` enhance hook) — 2+ modules needed the same prompt-enhance, so shape purity bends by design (§10) |
| `Badge` | `variant: neutral \| accent \| success \| danger` | Mono caption CHIP: `rounded-full border-white/10 bg-white/5 text-xs`, lowercase; text color = mist-dim / glow-amber / glow-green / glow-red |
| `Progress` | `value: 0–100`, `label?` | Flat `bg-glow-green` fill on the `bg-ridge` track, square ends, full ARIA |
| `PillGroup<T>` | `label`, `options`, `value`, `onChange` | Mono caption; selected = amber specimen tint, unselected = white/10 hairline → `hover:bg-ridge`; `aria-pressed` |
| `LangSwitch` | none | White/10 hairline pill group; active locale = `bg-ridge text-white` lit segment |
| `AppShell` | `user`, `isSessionPending?`, `onSignOut`, `balanceSlot?`, `children` | STICKY `bg-steel` bar, v3.1 COMPACT (44px: `py-1.5`, controls `min-h-8`, labels `text-xs`, wordmark `text-base` — chrome pixels go to the editor canvas); mono wordmark "openCreate·" (portal dot, aria-hidden); lowercase mono nav (active = white); Sign in = RED specimen pill; right cluster is `balanceSlot` + `LangSwitch` + `AccountMenu`. NOT the only top bar since 2026-07-23: the CinemaStudio editor (`/cinema/$filmId`) renders its OWN bar (`CinemaEditorHeader`) instead |
| `AccountMenu` | `user`, `isSessionPending?`, `onSignOut` | The header's right-most account slot, EXTRACTED from AppShell (2026-07-23) so the CinemaStudio editor bar can reuse it without a cross-module import. Three states: session-pending pill `Skeleton`, signed-out RED specimen Sign-in `Link`, signed-in `bg-ridge` disclosure menu (40px items — overlay, not chrome; Escape + click-away). Presentational; the route injects session + `onSignOut` |
| `AsciiSphere` | `className?` | Decorative aria-hidden `<canvas>`: animated ASCII ellipsoid in mist at 0.34 alpha on transparent bg; ~30fps rAF cap, cleanup on unmount, reduced-motion → static frame; caller sizes it (`absolute inset-0`) |
| `SpecimenTile` | `kind: 8 specimens (§5)`, `className?` | Decorative duotone SVG specimen plate (flat fills + patterns, no gradients); consumer owns the grid cell, fog border and caption |
| `SpendConfirmModal` **(moved into the kit 2026-08-20)** | `isOpen`, `title`, `description`, `confirmLabel`, `credits: number \| null`, `balance?`, `children?`, `onCancel`, `onConfirm` | THE blocking spend gate (§9), and now the app's only one. It lived in `modules/Assets3D` while that was the sole paid batch surface; the Shorts batch is the second, a module may not import another module's internals, and the last thing this codebase should own two of is the dialog that guards credits — so it moved here (ADR `shorts-studio` §4). `role="alertdialog"`, quiet ghost cancel, confirm that RESTATES the number. It owns almost no copy (callers pass localized prose) but it DOES own two decisions, because every caller must make them identically: `credits === null` disables the confirm — a null price is not a price — and a known `balance` short of `credits` disables it too AND states the shortfall (`spend.balance` / `spend.shortfall`, `role="status"`). `balance === undefined` disables nothing: a slow `/api/me` must never make the product look out of stock (the `TierPicker` rule). `children` is an itemisation slot — the "rows × beats × per-clip" breakdown a batch owes the user. The body scrolls, the actions are PINNED `shrink-0`: on a spend gate, a forty-row list pushing the confirm out of reach is the worst version of the Modal scroll bug. Callers close FIRST, then mutate |
| `AppErrorBoundary` | wraps the app | Crash → mono 400 30px headline on the void, one line, green pill reload |
| `OfflineOverlay` | none (self-managed) | Full-screen `role="alertdialog"` on the void; mono 400 headline; auto-clears on reconnect |
| `NotFoundPage` | none | Portal "404" status line, mono 400 30px headline, one green pill home link |
| `Toaster` + `toast` | `toast.error/info/success({title, description?, action?, durationMs?, dedupeKey?})`; `<Toaster>` mounted once in `__root.tsx` | The NON-BLOCKING notification surface (the counterpart to the blocking error modal, §9). A tiny Zustand store (`toastStore`) + a corner portal (bottom-center on 390px, bottom-right ≥sm, `z-[55]` — above Modal, below OfflineOverlay). Each toast is an OPAQUE STEEL card (a floating text/status surface is never frosted — unreadable over media, the Select-popup asymmetry) lifted with the existing `shadow-glass-lg` token, with a triad status LEFT RULE (`border-l-2` — red/portal/green, never a fill, never a gradient). Per-item live-region role (error = `alert`/assertive, info/success = `status`/polite); auto-dismiss PAUSES on hover/focus; async `action` shows a pending Button and dismisses on resolve; keyboard-dismissable close. Stack capped at 3 (oldest drops); `dedupeKey` collapses a re-fire to one live toast |

Buttons: primary/green = the single main create/submit action per view; ghost/amber =
secondary/quiet/retry/explore; danger/red = destructive + auth-entry per the reference
taxonomy. Size `lg` only for landing/hero CTAs. Links styled as the primary action
mirror Button primary classes (green specimen pill). Quiet text links are portal blue:
`text-portal underline decoration-portal/40 underline-offset-4 hover:decoration-portal`.

## 7. The 4-states rule (mandatory)

Every component/screen that renders server data implements all four states:

1. **Loading** — `Skeleton` blocks shaped like the eventual content (never bare spinners).
2. **Empty** — `EmptyState` with a next action.
3. **Error** — `ErrorState` with a localized, user-safe message + retry.
4. **Data** — the real render.

No blank screens, no raw error text, ever.

## 8. Accessibility rules

- Focus: `focus-visible:ring-2 focus-visible:ring-portal focus-visible:outline-none`
  on every interactive element (filled fields swap their hairline to `border-portal`
  instead — an equally visible replacement). Never remove outlines without a replacement.
- Hit area ≥40px (`min-h-10`); 44px+ on touch layouts. Icon-only buttons get `aria-label`.
- **An icon-only control that CARRIES A VALUE gets `aria-label` AND `title`** (2026-08-02,
  the composer's style/aspect chips). `aria-label` names it for AT; the native tooltip is
  the pointer user's only way to learn what a bare glyph does and what it is set to right
  now — write it as "what this does — current value". Icon-only ACTIONS (delete, expand)
  still need only the label. Where the value can be drawn, draw it: the aspect chip's glyph
  IS the ratio (a real rectangle, dashed when inherited), so the tooltip confirms rather
  than carries the whole meaning.
- Inputs always have visible labels; errors via `aria-describedby` + `role="alert"`.
- Status is never color-only: failed wells pair the glow-red border with text + chip;
  transaction amounts carry an explicit +/- sign.
- Triad glows never carry prose; portal is the only prose accent. Contrast table §2.
- Decorative art (`SpecimenTile`, `AsciiSphere`, ordinals, the section spark,
  the wordmark dot) is `aria-hidden`;
  headings/labels render the raw i18n strings (no CSS text-transform tricks needed —
  v3 has no uppercase anywhere).
- `<html lang>` mirrors the active locale; RU copy is complete, not partial.

## 9. Error-UX surfaces (frontend-error-ux contract)

| Surface | Component | Behavior |
|---|---|---|
| Unknown route | `NotFoundPage` | Portal "404" line + mono 400 headline + one green pill home. Root `notFoundComponent`. |
| Render crash | `AppErrorBoundary` | Mono "technical update" fallback + green pill reload. Technical detail → console/monitoring only. |
| Lost connectivity | `OfflineOverlay` | `useSyncExternalStore` on `online`/`offline`; full-screen `role="alertdialog"` blocker on the void; auto-dismisses. |
| Blocking failure | `Modal role="alertdialog"` + `ErrorState` | For failures requiring acknowledgement. Non-blocking failures stay inline (steel block + glow-red left rule). |
| Transient / informational failure | `toast` + `<Toaster>` (§6) | The NON-blocking, attention-grabbing surface for a failure the user should notice but not be halted by — e.g. a POLLED generation flipping to `failed`. Fires ONE toast (deduped per subject id); message via `errorCodeMessageKey(code)`, `content_blocked` gets rich `toasts.contentBlocked.*` copy + a "soften & retry" action; never raw server text. An inline record (e.g. `ShotClipStatus`) stays as the persistent belt-and-braces. |
| Destructive confirm | `Modal role="alertdialog"` + danger/ghost pills | Paid/irreversible actions never fire in one click: quiet mist sentence stating the consequence, DANGER specimen pill confirms, GHOST pill cancels (e.g. `gallery.deleteConfirm.*`). The mutation fires only on confirm. |

Copy rules: never raw server text, stack traces, or status codes; no red-primary panic
styling (red marks the status rule/text, never the whole surface); every error screen =
mono headline, one line, one action; both locales (`errors.*` keys).

Documented inline-style exception: `Progress` width % (runtime-computed). Documented
raw-hex exception: `specimenTileArt.tsx` (§5) + the `AsciiSphere` mist fallback. No others.

## 10. Governance

- Tokens live only in `theme.css` `@theme`; new tokens require a repeated product
  need, an entry in §2, and a note here. One-screen needs use existing tokens + opacity.
- New shared components: only when 2+ modules need them; add to §6 in the same task.
  Unreferenced kit components get DELETED, not kept "just in case" — but `Select`
  was REVIVED 2026-07-07 for the composer settings strip (it now backs the
  type/aspect/duration dropdowns in `ChatComposer`). The MODEL choice is the
  exception: a native `<option>` cannot carry a logo/tier/description, so the
  model dropdown is the module-owned custom `ModelSelect` listbox (§12), NOT the
  kit `Select`.
- Every v2 token (`cream`, `ink`, `ink-soft`, `vermillion`, `sand`, `media`,
  `success`, `danger`, `font-display`) is DEAD. Grep before reviving anything.
- Dark mode IS the mode — there is no light theme.
- Screens/routes: standalone screens (landing, login, 404, crash, offline) sit
  directly on the void; app screens run inside `AppShell` via the pathless `_shell`
  layout route. AppShell stays presentational (session + BalanceChip injected by
  `routes/_shell.tsx`).
- Prerender guard: `pnpm --filter @opencreate/web build` greps the hero/claims copy —
  redesigns must keep those strings rendering at `/`.

## 11. Page treatments (v3 restyle map)

| Page | Treatment |
|---|---|
| Landing `/` | Stage 2 done: FLOATING transparent masthead (nav only — pricing link, LangSwitch, session action; the wordmark moved into the hero); FULL-VIEWPORT (`min-h-svh`) hero = `AsciiSphere` behind the centered mono wordmark, kicker, whisper-weight headline with ONE portal accent word (EN "video", RU «копейки» — locale-driven via `landing.headlineAccent`, split inline so the h1 accessible name and the prerender grep stay intact), MIST claims line, and TWO specimen pills (green "Start creating" + amber "See pricing"); below: the ~800px research column (`max-w-[50rem]`) — specimen grid (§5), the mono terminal price index (glow-green "ours" numerals, PORTAL footnote), plain mono how-it-works prose rows (small portal ordinals), FAQ as prose on the void (no rules, no cards); minimal one-line footer (tagline + rights). Section headers = amber spark icon + mono 30px h2 (ordinals retired). |
| `/pricing` | Stage 2 done: the same ~800px research column; mono kicker + 30px h1 + glow-amber "200 free credits" chip; PriceTable (portal footnote) + ModelCreditTable with SPECIMEN-GREEN credit numerals under amber-spark SectionHeadings; visitor signup CTA as a `bg-steel` card with a green pill link. |
| `/login` | Stage 3 done: ONE centered STEEL card (`max-w-md rounded-lg border-white/10 bg-steel p-8`) on the void, under the mono wordmark home link (portal dot, accessible name "openCreate"); mono 30px h1 over a hairline, steel `Input` fields, portal mode-switch link, abyss + glow-red-rule server banner; submit pill tinted by mode — log-in = RED, sign-up = GREEN (reference taxonomy). Pending state keeps the same card silhouette. (The earlier manifesto split and `AuthManifesto` are gone.) |
| `/create`, `/library` | Stage 3 done: void canvas, sticky steel bar, mono 30px page h1s. Generator = the "commission sheet" (unfilled white/10 `rounded-lg` frame so the steel surfaces inside keep an elevation step; ghost mono `SheetField` ordinals; steel prompt textarea; glow-GREEN mono `CostLabel`; the custom `ModelSelect` listbox — logo + tariff + description, amber-ring selection). Gallery cards = figures on SQUARE `bg-abyss` tiles (8px radius) with the triad status colors — amber progress+%, green "ready" chip, red failure + refund note — and a glow-red ICON delete. |
| `/cinema` | CinemaStudio library: void canvas, sticky steel bar, mono 30px h1 + green "New film" pill; film cards = canvas-shaped `bg-abyss` plates (no cover → quiet film glyph) with title + aspect `Badge` + "updated" caption, the whole plate a `Link` into the editor; 4 states. |
| `/cinema/$filmId` | CinemaStudio editor, **v8 — a 70/30 TWO-COLUMN workbench** (owner request 2026-07-24, "2 блока в ряд"; the v7 fixed bottom-dock composer is RETIRED): under the top bar, `<main>` fills the height and holds a grid — LEFT 70% is the MONTAGE (the `PreviewPlayer` grows to fill the height with the transient `RenderBar` strip above it, over the `Timeline` TRACKS panel — "кадры, монтажный блок"), RIGHT 30% is the COMPOSER INPUT (`ShotInspector` — the selected shot's prompt + model/duration/cast/generate, "инпут где промтом создаём фильмы"; scrolls vertically). No page scroll: `main` is `overflow-hidden`, each column owns its overflow, and the grid fills the height via a flex chain (`flex-1`, not a `%` `h-full` that would not resolve against a flex-grown parent); below `lg` the columns STACK. **This route DROPS the global AppShell (2026-07-23, owner "раздел кино совершенно другой") and renders its OWN full-bleed top bar** — `CinemaEditorHeader` (v2 "Собранный бар", built to the §13 header laws), mirroring AppShell's 44px steel silhouette: a `‹` back link to `/cinema` (the escape hatch; the wordmark is dropped on a focused screen per §13.2), the DOMINANT film title as an INLINE-EDITABLE h1, a META row of quiet chips (a `16:9 ▾` aspect CHIP-DROPDOWN with the current value checked — not permanent toggles — · shot count · duration), a GREEN icon-led «Собрать mp4» primary button (disabled, not hidden, when it can't run), a ⋯ `Menu` for the rare actions (full settings modal for the style default + delete-confirm), a hairline divider, then the injected `chrome` cluster (balance · lang · account, composed by the route since the screen has no AppShell). Export state is still owned by `FilmEditor` (one truth for the button + the strip). Under the bar the STAGE scrolls inside itself: the TRANSIENT `RenderBar` status strip (CLIENT-side export since 2026-07-23 — idle = nothing; running → progress + % + cancel; done → green "saved"; blocked → named client refusal; unsupported → calm note), `PreviewPlayer` (fills the stage, PLAYHEAD-DRIVEN since v8 — the picture is whatever clip the timeline clock sits on, "approximation" caveat). The COMPOSER now lives in the RIGHT 30% column (`ShotInspector` — auto-grow prompt, label-less model chip → `ModelPickerModal`, stepped duration slider, speaker/cast/voice/expand toggles, Save + green Generate; slim hint row when nothing selected). Under the preview in the LEFT column is the TRACKS panel (`Timeline` v7/v8): a compact right-aligned ZOOM cluster (zoom out / fit / zoom in — the one control the panel wears), a RULER that doubles as a SCRUB SLIDER (m:ss timecode ticks at a zoom-chosen interval; click/drag/arrows seek), a thin portal PLAYHEAD CURSOR riding over the lanes at the clock's position, the VIDEO lane (`ShotThumb` slots as wide as their duration on one store-owned `zoom` px/sec scale; a tile click seeks to that shot AND selects it; hover-revealed TRIM edges + a REORDER grip drag-edit the shot via existing endpoints, with a green drop indicator and snap-to-boundary/playhead — Phase 3; a toolbar SPLIT scissors + keyboard shortcuts, Space/←→/Shift+←→/Home/End/S, cut and navigate — Phase 4), and the AUDIO lane directly beneath (music beds as amber bars to the film's end, voiceover chips at their startMs in green, hover 🗑) — all in ONE horizontal scroll so they cannot drift, and the strip AUTO-SCROLLS to follow the playhead during play. Preview + timeline share the singleton `useTimelineClock` (the NLE spine — `playheadMs`/`isPlaying`/`zoom`); playback is a rAF loop over the shots and the playing clip is AUDIBLE (its own soundtrack, muted again on pause/scrub). The «+» dialog adds shot · title · storyboard · music · voiceover (audio rows switch to a mini-form; one charged Generate). The «Звук» card is retired. Status is text + triad glow, never color-only. |
| `/soul` | AI Soul Studio, v2 — a 3-ZONE WORKBENCH (the `/cinema/$filmId` posture: `h-[calc(100svh-76px)]`, no desktop page scroll, zones scroll internally, composer pinned). LEFT RAIL: a "+ new character" reset over `SoulCharacters variant="rail"` (compact Link rows into the card; 4 states). CENTER STAGE: `SoulStage` — a "build your character" `EmptyState` while the draft is pristine, else a glass card with the name, the picked axes as neutral chips + traits as amber chips, and the composed `SoulPreview` well (no portrait — the face is minted later on the card, so the honest preview is the STRUCTURE + the exact prompt). RIGHT BUILDER: `SoulBuilder` — the shared `SoulAxes` (two required pills + eight optional `Select`s + capped amber trait chips + notes) under a ghost "start from a preset" that opens `PromptLibrary` in a steel `Modal`. BOTTOM DOCK: `SoulComposer`, a fixed `GLASS_FLOATING` capsule — a glass name field, a shuffle dice, the ONE green "Create character" pill + a "creating is free" caption. Below `lg` the zones STACK (rail → builder → stage). NOTHING here costs credits. |
| `/soul/$entityId` | The soul card: back link + name + amber "edit" (reopens the constructor in a `lg` `Modal`), then `SoulSheet` (four `PORTRAIT_SHEET_VIEWS` well plates, per-view shoot/reshoot pills, ONE green priced action — "First portrait · 2 cr" with no photo, "Complete the sheet · 24 cr" with one) and `SoulAnimate` (video model + duration `Select`s, an action prompt, a green "Оживить · N cr" pill, then the clip: amber `Progress` → well-plated `<video>` → glow-red reason + green "credits refunded" chip), with `SoulFacts` (a `<dl>` of picked LABELS + amber trait badges) in the right rail. Every paid action is confirmed by an `alertdialog` that repeats the price. |
| `/assets` | Modular 3D Assets library: void canvas, sticky steel bar, mono 30px h1 + green "New asset" pill; asset cards = glass `Card`s wrapping a SQUARE `well` plate that holds the real concept image (`object-cover` — an uploaded picture is CONTENT, never frosted), title + "created" caption, the whole plate a `Link` into the wizard; 4 states (4 square `rounded-2xl` skeleton silhouettes / `ErrorState` / `EmptyState` + green CTA / grid). |
| `/assets/$assetId` | The asset wizard — STAGE-shaped, not page-shaped: ONE route whose five acts (Концепт → Части → Извлечение → Меши → Сборка) are DERIVED from the part statuses, never routed. Tighter workbench canvas (`px-4 py-4 xl:px-6`, the `/cinema/$filmId` posture): portal back-link + mono 30px asset title, then `WizardStageNav` — a pill rail of five ordinals where the active step is the AMBER specimen tint plus `aria-current="step"` (never color-only), reached steps step to `bg-ridge` on hover, and steps ahead are `disabled` at 40% (hiding them would kill the orientation; letting them be clicked would drop the user on an empty screen). Under it the stage heading (`text-xl` white, weight 400) + a mist-dim description, then the stage body: Concept = the full concept in a `well` plate (`object-contain`, `max-h-[60svh]`); Assembly = the `React.lazy` boundary behind a plate-shaped `Skeleton` fallback (the ONLY place three.js is allowed to enter the graph). Prices are read from the catalog handed in by the ROUTE — a null price is a `Skeleton` + a disabled button, never a guessed number. |
| `/canvas` | Canvas Mode library: void canvas, sticky steel bar, mono 30px h1 + green "New canvas" pill; canvas cards = glass `Card`s (a board has no cover art, so the card is TYPOGRAPHIC — title + localized "updated" caption, the `Templates` reasoning: never a fake-media plate for something we cannot preview), the whole card a `Link` into the editor, hover lift ≤0.5 `motion-safe`; 4 states (3 skeleton silhouettes / `ErrorState` / `EmptyState` + green CTA / grid). |
| `/canvas/$canvasId` | The BOARD — the second route (after `/cinema/$filmId`) that DROPS the AppShell, because a canvas pans and must own the viewport (`h-svh`, no page scroll). Its own 44px steel bar per §13: `←` back to `/canvas`, the title as an INLINE-EDITABLE field (a board is named while you work, not in a create dialog), the autosave status as a quiet mist-dim `saved` / `saving…` caption that becomes an AMBER "not saved · retry" button on failure (never a toast — an autosave that shouts on every blip trains people to ignore it), then the injected balance chip. Below it the board: a 96px palette rail (six kind buttons — image · video · upload · character · prompt · note — drag OR click; `prompt` sits beside `character` because both are furniture WITH an output port, while a note has none) and the React Flow surface on the flat void with a `ridge` DOT grid (one surface step above the node cards, so texture reads as depth BEHIND them), minimap on `abyss`, controls bottom-left. NODES are OPAQUE steel `rounded-2xl` cards, not glass: they float over a textured board with wires and media behind them (the `Select`-popup argument, §3.5), and glass would fight the status border — the frosted recipe ships its own border-color utilities and Tailwind resolves competing ones by stylesheet order, not class order. |
| `/styles` | Style Studio: void canvas, sticky steel bar, mono 30px h1 + a mist-dim subtitle that says what a style IS (two prompt fragments and a name), green "New style" pill. Below it TWO labelled sections over ONE list — «Мои стили» first (the reason to be here), «Встроенные» second (a reference shelf you read before writing your own). Every tile is the same: a SQUARE `well` plate holding the style's own sample image (`object-cover`; no preview yet → a quiet mist-dim caption, never an empty plate — the `Entities` cover precedent), then the name over its fragment truncated to one dim line. A builtin closes with a neutral `Badge` and NO ⋯ menu at all (nothing a user may do to code; a disabled affordance would be worse); a user style closes with the ⋯ `Menu` (edit / red delete). The empty state is scoped to OWNERSHIP — the list is never literally empty — and deletion goes through a steel `Modal` whose danger pill restates the blast radius. 4 states (6 square `rounded-2xl` skeletons / `ErrorState` / `EmptyState` + green CTA / grid). |
| `/shorts` | Shorts Studio — the BATCH surface, **not** a second gallery (ADR `shorts-studio` §1: `TemplateCategory` widened by one value, so the shorts shelf already appears in `/templates` for free). Void canvas, sticky steel bar, mono 30px h1 + a mist-dim subtitle that says the deal out loud — creating the shorts is free, credits move only at the confirm. The screen is ordered as the DECISION is ordered: (1) the shelf as `TemplateCard` posters, reused verbatim from Templates; (2) once picked, a titled `Card` holding the format's shape line and the `TierPicker` — one tier for the whole batch, because mixing tiers inside one confirmed total is how an itemised price stops being itemised; (3) the `VariantsTable`; (4) a hairline-topped strip carrying the LIVE total (`text-lg` white tabular numeral over a mist-dim "N shorts × M beats × K cr" line) and the ONE green pill that restates it — `Run the batch · 180 cr`. A `null` total prints "Price unavailable" and DISABLES the pill, and the strip always says WHY it is dead (price drift · an incomplete row · a create failure) — a disabled control with no explanation is a dead end, and on a spend surface it reads as a broken product. While a run is in flight the pill is replaced by an amber `role="status"` line + a RED `Stop`: stopping is a recovery, and the design should nudge toward neither continuing nor quitting. Below it the `RunBoard`. The batch id rides in `?batch=…`, which is the whole reload story. |
| 404 / crash / offline | Done: mono 400 30px headline, one line, one action, flat void. |

## 12. Module UI surfaces (kept current per task)

Module-owned components (inside `modules/*`, composed from §6 primitives).

| Module | Surface | Composition & states |
|---|---|---|
| Auth | `AuthForm` (`/login`) | Inside the route's centered STEEL card: mono 30px h1 over a white/10 hairline, steel `Input` fields (border-defined on the card); submit pill tinted by mode — log-in = RED specimen pill (auth-entry), sign-up = GREEN (creating an account is a create action); login↔register switch as a portal link; zod errors per field (`role="alert"`); server banner = ABYSS block + glow-red left rule (steel would vanish on the steel card); Google button = amber ghost, behind `VITE_GOOGLE_AUTH=1`. |
| Credits | `BalanceChip` (header) | The AMBER SPECIMEN PILL at 32px (v3.1 compact bar; `text-sm` numeral): `rounded-full border-white/10 bg-specimen-amber/20 text-lumen-amber shadow-pill` (hover `/35` — Button-ghost anatomy: the balance is a real control, not passive meta), glow-AMBER bolt icon accent + `font-medium` numeral (the 500 ceiling); the bolt is a decorative `currentColor` SVG — NEVER an OS emoji; the numeral is COMPACT (§13.5 — `Intl` `notation:'compact'` → "1B"/"1 млрд", exact grouped value in `aria-label`+`title`, so a billion-credit dev account no longer blows out the chip); loading = pill `Skeleton`; failure = ↻ icon-button; signed-out hidden. Opens history modal. |
| Credits | `TransactionsList` (modal) | `Modal` (steel sheet) + 4 states; ledger rows on `divide-white/10`, mono 400 amounts — `+n` glow-green / `-n` glow-red (sign carries meaning, color reinforces). |
| Generator | `GeneratorPanel` (sheet posture) | The "commission sheet": a TITLED GLASS `Card` (`padding='lg'`, `title=generator.sheet`). v4 retired the unfilled `rounded-lg border-white/10` frame — the card now has depth of its own, so the steel inputs inside no longer carry the elevation ladder alone — and retired the `<header>` + `aria-label` pair with it: the visible mono caption IS the landmark's accessible name (which therefore changed from `generator.title` to `generator.sheet`). Ordered `SheetField` rows (`divide-white/10`), glow-GREEN mono `CostLabel` + green Generate against the closing hairline (price and action share the create tint). Catalog 4 states. Submit failures inline via `SubmitErrorBanner` (portal `/pricing` link on insufficient credits). Sibling of `ChatComposer` over the same store — `/create` renders the composer, whose prompt carries the shared `EnhanceButton` (enhance→replace + undo, §6). |
| Generator | `SheetField` | One sheet row: ghost mono ordinal (`text-white/20`, 400, aria-hidden, derived from render order) + field group; separators owned by the parent `divide-y`. |
| Generator | `PromptField` | Mono caption + steel filled textarea (`bg-steel rounded-lg`, focus → portal border) — the `Input` treatment's textarea twin. |
| Generator | `SubmitErrorBanner` | `role="alert"` steel block + glow-red left rule; EVERY envelope code renders localized primary copy — `insufficient_credits` (+ portal `/pricing` link) and `content_blocked` (refund promise) keep dedicated wording, all other/unknown codes map via `shared/libs/errorCopy` → `errors.codes.*`; the raw envelope message may trail only as a secondary `text-mist-dim` line (suppressed for the two dedicated codes and for non-envelope exceptions). |
| Generator | `ModelSelect` (+ `ModelSelectOption`, `ProviderMark`, `useModelListbox`) | Custom accessible LISTBOX (not a native `<select>` — options need a logo, tier chip and description). Trigger = steel (`variant='sheet'`) or glass (`variant='glass'`, composer) field showing the selected model's `ProviderMark` + name + provider label + `N cr`. Opens an OPAQUE `bg-steel` panel (readable over the composer's frosted glass — the reason a native select was rejected here), placement-aware (flips UP for the bottom-docked composer), listing ALL models in `role="group"` sections (Images / Video). Each `role="option"` row: `ProviderMark` tile, name + provider, tier `Badge` (amber), tariff `N cr · $N.NN` ($ = credits × $0.01, video quotes its base duration) and a localized description (`generator.models.<id>.description`). SELECTED = amber ring (`ring-glow-amber/60`) + `aria-selected`; ACTIVE (keyboard/hover) = one step to `bg-ridge`. Full keyboard: Arrow/Home/End move, Enter/Space select, Esc closes + restores focus, typeahead by name; click-outside closes; enter transition is CSS `@starting-style` (no gradient). Owns `useCatalog` + the 4 states (skeleton / inline retry / disabled placeholder / data). Picking a video model while on 'image' flips the type via the store's `normalizeFor`. Replaced the old `ModelPicker` tiles (deleted 2026-07-08). |
| Generator | `ProviderMark` | Inline SVG provider LOGO marks (one crisp monoline glyph per brand: flux/pixverse/minimax/seedance/wan/kling/veo + generic) — self-contained (no external URLs, CSP-safe), `currentColor`, flat strokes, no gradients; `aria-hidden` (the brand name is text). |
| Generator | `ImageDrop` | Dashed white/15 dropzone on the void (hover brightens + ridge wash) + sr-only file input; preview thumb on `bg-abyss`; image/* ≤10MB; glow-red inline errors. |
| Generator | `AttachImage` (+ `ChatComposer` paste/drop) | The docked composer's compact iOS-style paperclip: a 40px "+" (a sr-only `image/*` input) that collapses into a round thumbnail chip + glow-red overhang ✕ once an image is chosen — only where `model.supportsImageInput`. Since 2026-07-22 the CLICK is joined by two capsule-level gestures: a screenshot **paste** (`onPaste` bubbles from the textarea) and a **drag-drop** anywhere on the `ChatComposer` `<section>` set the SAME `inputImage`, all through the ONE shared `shared/libs/readImageFile` gate (≤10MB, image/* — the caps never fork). A drag-over paints a `ring-portal`; a gate reject is a localized `role="alert"` line; a text paste (no clipboard files) is left untouched. Element-scoped React handlers, so no document listener leaks. |
| Gallery | `GalleryGrid` | 4 states: 8 plate-shaped skeletons (`animate-skeleton`, `rounded-2xl` = the well's radius, so the grid never re-corners itself when data lands) / `ErrorState` / `EmptyState` + green pill `/create` CTA / 1-2-3-col grid + amber ghost Load more. |
| Gallery | `GenerationCard` | A FIGURE: a SQUARE `Card surface='well' padding='none'` plate — **never `glass`**. A thumbnail in a grid IS the content; frosting a card around a photo lays a lit edge, a blur and a shadow over what the eye is reading as an image. Glass is for chrome that floats OVER media. Images AND videos are `object-cover`; the full frame lives in the detail viewer. **The video plate is a POSTER, not a player (2026-08-02, owner request):** `preload="metadata"` + a `#t=0.1` media fragment paints the first frame (without the fragment Chrome renders a BLACK element), `muted playsInline pointer-events-none aria-hidden`, with a centred `void/60` play disc and a bottom-left duration chip (`gallery.seconds`) over it — the whole tile is the same open-the-viewer button an image has. The retired `<video controls>` cost three things: the browser's grey control bar was the one piece of grid chrome the design system does not own, a controls-bearing video swallows the click (so a clip was the ONLY card that could not be opened), and `controls` is a focus stop that put the player's internals ahead of the card's own actions in the Tab order. The `<button>` sits INSIDE the well (a `<button>` takes phrasing content, `Card` renders a `<div>`), so the plate owns the `motion-safe` hover lift and the button a `focus-visible:ring-inset` ring — an outset ring would be clipped by `overflow-hidden`. Mono mist prompt caption; the action set rides the plate's corner in a `Menu` (glow-red ICON delete — `#ff2056` is "icons/status only", a red pill per figure shouted). Two plates stay hand-rolled at the well's radius: PROCESSING (`animate-skeleton` walks background-color; a well would pin the fill and kill the pulse) and FAILED (its `border-glow-red` is STATUS, and Card owns its hairline as surface — pushing a border color through the layout-only `className` would let Tailwind's stylesheet order decide the winner). Processing = `animate-skeleton` tile + `Progress` + glow-amber %; polling is BOUNDED — 20 min past `createdAt` the interval stops and a `role="status"` glow-AMBER "taking longer than usual" note (`gallery.stalled`) + amber ghost Refresh pill (`gallery.refresh`, one manual poll) appear under the progress row (amber, not red: nothing failed yet); a status poll that fails before any data replaces the tile with `ErrorState` (`gallery.pollFailed`) + retry — the card never freezes at "Generating N%". Succeeded = green "ready" chip (`gallery.ready` — status said in text, never color-only); failed = glow-red hairline tile + a localized primary reason (`errorCodeMessageKey(errorCode)` → `errors.codes.*`, unknown → generic) + the raw provider text only as the secondary mist-dim caption (suppressed for `content_blocked`) + "Credits refunded" green chip. Plates lift on hover (`motion-safe`). Delete opens the destructive-confirm alertdialog (§9, `gallery.deleteConfirm.*`) — the optimistic removal fires only on the danger-pill confirm. |
| Gallery | `GenerationDetail` (the VIEWER) | **v2, 2026-08-02 — full-screen, two-pane** (owner: "модалка на весь экран… удобная и красивая", "блок с информацией промта и модели"). A `Modal size='full' surface='glass' hideHeader` sheet holding a grid: at ≥lg `minmax(0,1fr) 22rem` — LEFT the media STAGE (a `Card surface='well' padding='none'`, `object-contain`, so the frosted dialog floats and the user's work is sunk one step into it), RIGHT the INFORMATION column, which owns its own scroller; below lg they stack and the body scrolls (a 380px split is two unusable columns). The column reads: `gallery.detail.info` caption + green "ready" `Badge`, the ⋯ `Menu`, the PROMPT in its own `well` plate (`break-words`), then a `<dl>` of six `tabular-nums` facts — model (NAMED from the catalog the ROUTE injects as `models`; the raw `modelId` is the fallback, and a retired model still answers "what made this"), type, aspect, duration, cost, created. Video keeps `controls` + `autoPlay` HERE (the click that opened the sheet is the user activation the autoplay policies want) — that is the point of the grid's poster plate. **No icon rail:** every action is behind the ⋯, because Delete is irreversible and sat at Download's visual weight directly under the media (§13.2). The ⋯ is sheet CHROME, not column content — `absolute top-6 right-[4.5rem]` on the panel, pairing with Modal's own `top-6 right-6` close button: 24 + 40 = 64px is where the ✕ ends, so 4.5rem is the first offset that clears it by the §13.1 8px minimum. **A `hideHeader` sheet that wants its own overflow control must anchor it this way** — put it in the body's header row instead and you get two 40px circles at zero gap with a 4px vertical offset (measured, and the owner's "кривая вёрстка" the same day). The column's header row then buys `lg:pr-28` clearance, only at ≥lg (stacked, the cluster floats over the media instead). The list is the shared `useGenerationActions`, wrapped one level so this view adds "…then close the sheet" for `regenerate` without the action set learning a viewer exists. `pr-12` on the header row clears the sheet's floating close button. Only layout classes go through a Card's `className`; the surface is Card's. |
| Gallery | `GalleryFilterChips` | `PillGroup` All/Images/Videos (amber selection). |
| Gallery | `ViewSettingsMenu` | Gear disclosure holding the display preferences (grid/table/slide + density). Its panel stays an OPAQUE `bg-ridge` step, **not** glass: a translucent popup over moving media is unreadable. Same asymmetry `Select` documents. |
| Entities | `EntityLibrary` (`/entities`) | 4 states over `useEntities`: 6 square `rounded-2xl` skeletons / `ErrorState` / `EmptyState` + green create pill / grid of cover tiles. A tile is a `Card surface='well' padding='none'` (an uploaded face is content, not chrome — same reasoning as `GenerationCard`) holding a media button, with the name, localized kind and an overflow `Menu` beneath. |
| Entities | `EntityEditor` (modal) | `Modal` create/edit (null=create): steel `Input` name, kind `Select` (create only — kind is immutable), steel textarea + preset snippet chips, and in edit mode the photo set with amber-bordered primary election. Inline glow-red `role="alert"` errors. |
| Landing | `LandingPage`, `Hero`, `ShowcaseSpread`, `SectionHeading`, `PriceTable`, `HowItWorks`, `FaqClaims`, `ModelCreditTable`, `TableScrollRegion` | Stage 2 terminal skin (§11): full-viewport `AsciiSphere` hero with two pill CTAs; `ShowcaseSpread` = ONE figure wrapping the 4×2 `SpecimenTile` grid + ONE honest figcaption (sampleLabel chip + `landing.showcase.caption` naming the real models) + one video-marked tile; `SectionHeading` (amber spark icon, decorative — heading names are behavior) and `PriceTable` (no props since Stage 2) are also consumed by the /pricing route via the module index. Claims and honesty markers ("verified July 2026" caption as the table's accessible name, one named competitor per row, the sample-style labeling) are unchangeable. `TableScrollRegion` (v4 QA r2 + 390px affordance) wraps both index tables: a keyboard-focusable `role="region"` overflow area + a dynamic aria-hidden mono `common.scrollHint` ("scroll →" / «прокрутите →») + a translucent SOLID `bg-void/60` right-edge overlay strip (aria-hidden, pointer-events-none) that retires at the far right — the NO-GRADIENT answer to an edge fade, both shown only while columns really overflow. |

| Cinema | `CinemaLibrary` (`/cinema`) | 4 states over `useFilms`: 4 canvas-shaped `Skeleton` plates / `ErrorState` / `EmptyState` + green "New film" pill / grid of `FilmCard`. Header green "New film" opens `FilmSettingsModal` (create). |
| Cinema | `FilmCard` | A `Link` figure: canvas-shaped `bg-abyss` plate (aspect-video/square/9:16) with a quiet film glyph (no list cover), mono white title, neutral aspect `Badge`, mist-dim "updated {date}" (localized `Intl`). Plate lifts ≤0.5 on hover (`motion-safe`). |
| Cinema | `FilmSettingsModal` | `Modal` create/edit (null=create): steel `Input` title + aspect `PillGroup` + default-style `Select` (STYLE_PRESETS + "no style"). Create navigates into the new editor; edit closes. Error → inline glow-red. |
| Cinema | `CinemaEditorHeader` | The editor's OWN full-bleed steel top bar (replaced `FilmEditorHeader` + the global AppShell on this route, 2026-07-23; **v2 "Собранный бар"** applies the §13 header laws). `{ film, onExport, canExport, isStarting, shotCount?, durationMs?, chrome? }`. LEFT ZONE: `‹` back → `/cinema` (icon-only, the wordmark was dropped per §13.2), then the DOMINANT title as an inline-editable h1 (`text-base` white button → autofocused `bg-steel` input; Enter/blur commits a non-empty change via `useUpdateFilm` PATCH `{title}`, Escape reverts), then a META row of quiet `text-xs text-mist-dim` chips: a `16:9 ▾` **aspect chip-dropdown** (§13.3 — a `Menu` whose current value is CHECKED with a glow-amber tick + same-size spacers, PATCH `{aspectRatio}`; NOT the old permanent toggles) · `N shots` (`cinema.header.shots` plural) · `m:ss` (`formatTimecode`). RIGHT ZONE: the ONE primary — GREEN icon-led `Button` «Собрать mp4» (`PlayIcon`, `size='sm'`, disabled when `!canExport`, spinner while `isStarting`) — then a ⋯ `Menu` (settings → `FilmSettingsModal` edit for the style default; delete → §9 alertdialog → `useDeleteFilm` then navigate `/cinema`), a hairline `│` divider, then the injected `chrome`. `film` undefined → title `Skeleton`, no meta/menu, export disabled. Rendered by `FilmEditor` (owns export state + computes shotCount/durationMs); `chrome` injected by the route (Cinema imports no Auth/Credits). |
| Cinema | `Timeline` | The v7 TRACKS well (ruler + video lane + audio lane on one `PX_PER_SEC=24` scale, one horizontal scroll) plus the "+" dialog + keyboard height separator. v8 (NLE Phase 1) adds the playhead spine, all on the shared `useTimelineClock`: the RULER became a `role="slider"` SCRUB surface (pointer down = jump, drag while a button is held, Arrow/Home/End nudge; position exposed as TIME in `aria-valuenow`); a thin `bg-portal` PLAYHEAD CURSOR (a design token, NOT a gradient; `aria-hidden`, `pointer-events-none`) rides over the lanes at `msToPx(playheadMs)`; a shot-tile click `selectAndSeek`s — it opens the composer AND seeks the playhead to that shot's start (selection unifies with the playhead, per the ADR). Seeks clamp to the film's real length (`totalDurationMs`), so a click in the ruler's padded tail parks on the last frame. v8 Phase 2 makes the scale a store-owned `zoom` (px/sec, replacing the old `PX_PER_SEC` constant): a compact right-aligned ZOOM CLUSTER (hairline `size-7` tool buttons — zoom out / fit / zoom in, `hover:bg-ridge`, portal focus ring, `role="group"`; the one control the "no chrome row" panel wears, because a 10-minute film needs it), m:ss ruler TIMECODES at a zoom-chosen interval (`formatTimecode`/`rulerTicks`), horizontal scroll of the tile strip, and auto-scroll that FOLLOWS the playhead during play (a manual scroll while paused is respected). Fit measures the strip's `clientWidth` and asks the store; the tile 56px min-width floor is kept for clickability (a cosmetic divergence from true scale only at extreme zoom-out — the ruler + cursor stay exact). v8 Phase 3 adds ON-TIMELINE EDITING (existing endpoints): each tile carries `ShotTileAffordances` (hover-revealed left/right TRIM edges + a REORDER grip), driven by the `useShotDrag` hook — trim → `useUpdateShot` PATCH `{trimStartMs, durationMs}`, drag → `useReorderShots` POST the moved order; a green `bg-glow-green` DROP indicator (distinct from the portal playhead) marks the target slot; trim/reorder SNAP to clip boundaries + the playhead. All drag math is pure in `timelineGeometry`. v8 Phase 4 adds a SPLIT-at-playhead scissors (in `TimelineToolbar`, enabled only when the playhead is strictly inside the selected shot → `useSplitShot` POST `/shots/:id/split {atMs}`) and editor KEYBOARD shortcuts via `useTimelineKeys` (Space play/pause · ←→ frame step · Shift+←→ shot-boundary jump · Home/End · S split), suppressed while typing in the composer. |
| Cinema | `ShotThumb` | Fixed 16:9 `bg-abyss` tile reflecting its clip's LIVE status (shared `['generation', id]` cache): null → title/placeholder glyph · processing → `animate-skeleton` · failed → glow-red text · succeeded → video first-frame / `object-cover` img. Portal ordinal chip, void/70 duration chip, amber crossfade edge marker, amber-ring when selected; move (chevrons, end-disabled) + glow-red delete beneath. |
| Cinema | `TimelineToolbar` | The timeline's compact right-aligned tool cluster (extracted from `Timeline` in Phase 4 so that file stops growing) — hairline `size-7` tool buttons: SPLIT scissors (Phase 4, `disabled` at 40% unless the playhead is strictly inside the selected shot) + ZOOM out / fit / in (Phase 2). Purely presentational; every action + the `canSplit` gate is decided by `Timeline` (which owns the clock, selection and mutations) and passed in. The one chrome the "no chrome row" panel wears. |
| Cinema | `ShotTileAffordances` | v8 Phase 3 drag handles riding ON a tile (SIBLINGS of `ShotThumb` in the lane `<li>`, which is `group relative` — `ShotThumb` untouched, tile centre still selects): two hover-revealed TRIM edges (left in-point / right out-point, `w-2` `cursor-ew-resize`, tint `bg-portal/40` when grabbed, inner `bg-white/70` bar) + a top-centre REORDER grip (`cursor-grab`, `bg-void/60` scrim). Focusable `<button>`s with per-shot `aria-label`s; pointer-drag only (keyboard reorder stays on the chevrons, keyboard trim is a Phase-4 gap). Presentational — the `useShotDrag` hook owns the session + commits. |
| Cinema | `ShotInspector` | The selected shot's editor (keyed by shot.id): steel prompt textarea, `PresetPickers`, video-model + duration `Select`s, transition (+ crossfade length) `Select`s, optional title overlay (amber toggle + text + position), an abyss "the model will see" hint (`applyPromptPreset`), a text+glow status line, ghost Save + green Generate (spark icon). Generate saves then creates+links the clip. The shared `EnhanceButton` (§6) docks in the prompt field's bottom-right corner (enhance→replace + undo), wired to the same draft `setPrompt` so cast tokens and the resize grip are untouched. `useShotFailureToast` (model) raises ONE toast when the selected shot's polled clip flips to `failed` (the inline `ShotClipStatus` line stays the quiet record); `content_blocked` carries a "смягчить и повторить" action → `createSoftenRetry` (POST `/api/prompt/enhance` `mode:'soften'` → PATCH prompt → regenerate; degrades to a manual-edit toast if the endpoint is absent). The SUBMIT mutation retries transient codes 1–2× (`shouldRetrySubmit`); the four actionable/terminal codes never retry. **The model chip LOCKS to the style (2026-08-02):** when the active style's registry row names a `recommendedModelId` the catalog actually offers, that model becomes the effective one and the chip goes `disabled:opacity-40` with a `title` naming the style that decided — the pairing is the style's whole point, so the picker must not pretend to still be a choice. Derived every render (no `useEffect`), so it follows the style chip LIVE; it SHADOWS the user's own `modelId` rather than overwriting it, so clearing the style returns their pick, not the recommendation. A recommendation the catalog cannot serve is no lock at all. |
| Cinema | `ShotReferenceImages` | In the 📎 cast drawer, BELOW `ShotCastField` — the "attach any picture" affordance (vs the cast field's "tag a character"), sharing the budget of 5 (`entityRefs` + images, `MAX_SHOT_REFERENCE_IMAGES`). THREE inputs → ONE gate: click (a `<label>` wrapping a sr-only `image/*` input), drag-drop, and paste (Cmd/Ctrl+V) all route through the shared `shared/libs/readImageFile` and POST the data URI (`useAddShotReference`). Attached refs render as a `Card surface='well' padding='none'` thumbnail grid (`<img src>` = the refetched shot's `/media` path, NEVER the client data URI), each with a glow-red overhang ✕ (`useDeleteShotReference` by id). 4 states: Loading (a `Skeleton` plate while the upload is in flight) · Empty (the click/drop/paste hint) · Error (a client type/size reject OR a server 400 → `errorCodeMessageKey`, never raw text) · Data (the grid). Counter "N / 5" (amber at the cap, where the add tile hides). Paste is element-scoped React `onPaste` on the focusable (`tabIndex=0`) region — no document listener, so nothing leaks — and honest capability copy (`cinema.shotRef.modelHint`) when the model lacks `referenceMode`: it does NOT block attaching (the refs persist and pay off on switch to Wan 2.7), it just says so. |
| Cinema | `PresetPickers` | The shot's LOOK controls, in two registers (v2, 2026-08-02, owner: «два контрола иконками, с тултипами понятными»). STYLE and the per-shot ASPECT RATIO are ICON CHIPS — `Menu` chip-dropdowns (§13.3: current value CHECKED, leading check + same-size spacer) on the composer toolbar's `size-8` chip scale, amber-tinted when set, each carrying `aria-label` + a `title` tooltip (§8) because the trigger has no text. The style chip is a palette glyph; the ASPECT chip's glyph IS its value — `FrameIcon` draws the actual rectangle, DASHED when the shot inherits the film canvas. Framing/motion/quality stay labelled `Select`s in a grid below, built FROM the contract preset tables (style rows come from the style REGISTRY and prepend a "no style" row; modifier axes carry their own "any"). Aspect is deliberately NOT part of `PresetDraft` (it never reaches `promptPreset` — it is a generation param); it rides its own prop + callback, `null` meaning "no opinion, inherit the film". |
| Cinema | `PreviewPlayer` | PLAYHEAD-DRIVEN stage (v8, NLE Phase 1) on a stage-filling `well`: reads the shared `useTimelineClock`, resolves the clip at `playheadMs` (`clipAtMs`) and mounts its video/img/title-slate, seeking `video.currentTime` to the offset within it (frame-accurate — a tile click or ruler scrub makes the picture jump). Play is a `requestAnimationFrame` loop advancing the playhead across clip boundaries, parking+pausing at the film end, cancelled on pause/unmount (no leak). Amber play-pause pill + `n / total` + mist-dim "preview is an approximation" caveat. Reads clips from cache (no own poll). Replaced the old sequential `index`/`ended`-timer player. v8 Phase 2: SOUND ON PLAY — `video.muted = !isPlaying` (imperative), so the clip's own soundtrack is audible while playing and silent while paused/scrubbing (the play gesture satisfies autoplay). |
| Cinema | `RenderBar` | The CLIENT-export status strip (2026-07-23, client-side-export ADR — the export runs in the browser via WebCodecs now). Idle = nothing. RUNNING → `Progress` + glow-amber % + a ghost CANCEL (a real encode to abort). DONE → a quiet glow-green "saved" (the mp4 downloaded or streamed to disk — no served `/media` link). ERROR → calm `ErrorState` retry (never raw encoder text). BLOCKED → amber `role="alert"` naming the client refusal (a clip generating/failed, no shots) + optional "Show me", NO retry pill (a block isn't a failure). UNSUPPORTED → a calm amber note where WebCodecs/File System Access is missing. Pure presentation; `useExportController` (`useFilmExport` pipeline + `computeExportBlock` validation + capability gate) owns the state. |
| Cinema | `useExportController` / `useFilmExport` / `computeExportBlock` | The client-export model layer (not a component). `useExportController` composes the pipeline hook + the client validation into the one API the header CTA + `RenderBar` read; `useFilmExport` runs the lazy WebCodecs pipeline (progress/cancel/4 states); `computeExportBlock` reuses `renderBlockCopy` to refuse a not-ready film with the same named reasons the retired server gave. The mediabunny adapter (`filmExporter`/`filmFrameDrawer`/`filmExportAudio`) is a lazy chunk, off the main bundle. |
| Cinema | `AudioTracks` | Amber ghost "add music"/"add voiceover" → mini-form (prompt + tts voice `Select`) generates+links an audio track; track rows (music/mic icon + kind, glow-red remove) on `divide-white/10`. Hidden affordances when the audio models are absent. |
| Cinema | `StoryboardModal` | `Modal`: steel script textarea + style + shot-count `Select` → POST storyboard (draft shots appear on reload). A clean `provider_error` renders the amber "storyboarding isn't configured" `role="status"` inline — never a crash. |

| Soul | `SoulConstructor` (edit modal) | Since the /soul v2 recomposition (2026-07-21) this is the EDIT-MODAL form only (`SoulEditModal`): a titled GLASS `Card` with the steel `Input` name, the shared `SoulAxes` body, `SoulPreview`, and a green submit pill beside the mist-dim "free" caption. CONTROLLED — the parent owns the draft. DOM order is unchanged (name → axes → preview → submit), which is why its test stayed green. Failures = steel block + glow-red LEFT RULE (`errorCopy`, never raw text). |
| Soul | `SoulAxes` | The shared, CONTROLLED axis body extracted from the constructor so the studio's Builder and the edit modal cannot drift a picker apart: `PillGroup` for the two REQUIRED axes (archetype, style — a dropdown for a choice with no empty state hides it), a 2-col grid of `Select`s for the eight optional axes (each with an "any" row), `TraitPicker`, the steel notes textarea. NAME-LESS — the two owners frame name/submit differently. Options/trait/style LABELS come from the contract tables and are the documented i18n exception (DATA, like `STYLE_PRESETS`). |
| Soul | `SoulBuilder` (`/soul` right) | Titled GLASS `Card` "Builder": the shared `SoulAxes` under a ghost "start from a preset" header action that opens `PromptLibrary` in a steel `Modal` (glass-on-steel, never glass-on-glass). Only the SOUL travels (the name is the composer's); `onPreset` still writes a whole `Soul` literal into the studio draft. |
| Soul | `SoulStage` (`/soul` center) | The live draft, TWO states (it is CLIENT data — the 4-states rule governs SERVER data): a "build your character" `EmptyState` while `isDraftPristine`, else a glass card with the name (or an "unnamed" placeholder), the picked axes as neutral `Badge`s + traits as amber `Badge`s, and the composed `SoulPreview` well. No portrait — a face is minted later on the card; the honest preview is the structure + the exact prompt. |
| Soul | `SoulComposer` (`/soul` dock) | The ONE create action: a fixed `GLASS_FLOATING` capsule (chrome hovering OVER scrolling content) mirroring the Cinema dock — a translucent white/5 glass name field (aria-labelled), a shuffle dice (decorative `currentColor` SVG, NEVER an emoji — calls `randomizeDraft`), the single green "Create character" pill (disabled until `isDraftReady`) + the mist-dim "free" caption, and the create failure as a steel/glow-red-rule alert. |
| Soul | `TraitPicker` | Chip groups from `TRAIT_GROUPS`: picked = amber specimen tint + `aria-pressed`, idle = white/5 wash → `hover:bg-ridge`, and at `MAX_TRAITS` (6) every unpicked chip is DISABLED (40% dim) with a glow-AMBER `role="status"` line saying why — amber, not red: nothing failed, the studio is protecting a render the user is about to pay for. A picked chip stays enabled at the cap, or the user is stuck at six. |
| Soul | `SoulPreview` | Recessed `well` Card, "what the model will see": the composed positive prompt (mono mist) + the negative as a quiet mist-dim caption + an amber Copy pill. Composed by the CONTRACT functions (`composePortraitPrompt` + `applyPromptPreset`), so it cannot drift into a plausible lie. |
| Soul | `PromptLibrary` | Glass card holding one recessed `well` per `PROMPT_LIBRARY` entry: white label, 3-line-clamped composed prompt, amber Copy + amber "open in constructor" (free, because each entry is a `Soul` literal, not a string). Since /soul v2 it is reached from the Builder's "start from a preset" steel `Modal` rather than a standalone rail; `onOpen` writes the whole `Soul` + name into the studio draft, unchanged. |
| Soul | `SoulCharacters` | 4 states over the SHARED `['entities']` query filtered to `soul != null`, in TWO layouts (`variant`): `grid` (6 square `rounded-2xl` skeletons → `ErrorState` → `EmptyState` → `Card surface="well" padding="none"` plates) and `rail` (the studio's left column — compact skeleton rows → same error/empty → Link rows: a small abyss thumb + name). ONE query, both layouts: the loading/error/empty branches are shared, only the DATA branch forks. Each row/plate is a typed `Link` into the soul card with an INSET focus ring (paid minting stays off `/soul`); a cover photo is CONTENT — never frosted glass. |
| Soul | `SoulSheet` (`/soul/$entityId`) | THE paid surface. Four well plates in `PORTRAIT_SHEET_VIEWS` order (label from `PORTRAIT_VIEWS`); minting plates pulse with `animate-skeleton`. ONE green priced action — "First portrait · 2 cr" (no photo → `flux-dev`) or "Complete the sheet · 24 cr" (photo → 3 × `flux-kontext-pro`) — plus per-view amber shoot/reshoot pills once a photo exists. The price is READ FROM THE CATALOG and a `null` price DISABLES the button (a pulse, never a guessed number). Every mint passes through a `Modal role="alertdialog"` repeating the cost (§9). A failed view shows the localized `errorCode` reason + a green "credits refunded" `Badge`; a whole-call failure is an inline steel/glow-red-rule alert. |
| Soul | `SoulAnimate` | The 35–140-credit act, never automatic: video-model + duration `Select`s, an editable action prompt (seeded from the derived description), a green "Оживить · N cr" pill behind an alertdialog. With no primary photo it renders a mist-dim reason instead of a dead button. The clip's states: amber `Progress` + glow-amber caption → `<video>` in a well plate (`object-contain`, never cropped) → glow-red localized reason + green "credits refunded" chip → amber `gallery.pollFailed` line. |
| Assets3D | `AssetLibrary` (`/assets`) | 4 states over `useAssets`: 4 square `rounded-2xl` skeleton silhouettes (plate + two meta lines, so the grid never reflows) / `ErrorState` / `EmptyState` + green "New asset" pill / grid of `AssetCard`. The header pill is GREEN, not ghost: it is the single create action on the screen and it is FREE — the prices start at the wizard's extraction stage. `CreateAssetModal` is mounted only while open, so an abandoned attempt cannot leave a stale data URI in form state. |
| Assets3D | `AssetCard` | A `Link` figure: glass `Card` wrapping a SQUARE `Card surface="well" padding="none"` that holds the concept image (`object-cover`) — an uploaded picture is CONTENT, so it is never frosted (the `GenerationCard` rule) — then a white title and a mist-dim localized "created {date}". Plate lifts ≤0.5 on hover (`motion-safe`). No "missing image" branch exists: the API refuses a create without a concept. |
| Assets3D | `CreateAssetModal` | `Modal` (steel sheet): RHF + zod steel `Input` title, then a real `<label>` wrapping a `sr-only` file input — an unpicked state is the dashed white/15 dropzone on the void (the `ImageDrop` voice), a picked one is the preview in a `well` plate (`object-contain`, `max-h-56`). The file is read to a base64 data URI CLIENT-SIDE (the API never accepts a user URL — SSRF), and svg is refused before it is decoded (a script container, not a picture; the contract's regex admits png/jpeg/webp only). Zod messages are i18n KEYS translated at the render site. Server failures = abyss block + glow-red LEFT rule via `errorCopy`, never raw text. A mist-dim caption says creating is free, because an upload box implies a charge is coming. |
| Assets3D | `WizardStageNav` | The five-step rail: an `<ol>` of pill BUTTONS (not links — a stage is derived state, not a route), each led by a decorative portal ordinal. Active = amber specimen tint + `shadow-pill` + `aria-current="step"`; reached = white/10 hairline → `hover:bg-ridge`; ahead = `disabled` at 40%. `active` and `reached` are two separate props so walking BACK never locks the user out of walking forward. |
| Assets3D | `PriceTag` | The module's local price pill: `credits: number \| null`, where `null` renders `<Skeleton className="h-3 w-10"/>` and the caller disables the button. A deliberate second copy of Soul's private helper — flagged for a `shared/ui` extraction now that two modules need it (§10). |
| Assets3D | `PartsStage` | The FREE act. Glass `Card`: a ghost analyse pill (no dialog — a modal in front of a costless act only trains the click-through reflex the paid stages depend on the user NOT having) over an `<ul>` of `PartChecklistItem`, then the by-hand add row (steel `Input` + green pill, commits on Enter) and the `{{total}} of 12 parts` counter. At `MAX_PARTS` the add pill disables AND an amber line SAYS why (VRAM) — a disabled control with no reason reads as a bug. A `provider_error` from analyse degrades to a steel/glow-red-rule inline notice, never a dead stage: the wizard must stay fully usable with no `ANTHROPIC_API_KEY`. The forward CTA carries the extraction `PriceTag` beside it, so the first time the user sees a number is not the moment it is charged. |
| Assets3D | `PartChecklistItem` | One `<li>` on a white/10 hairline: read mode = name + dim description + amber edit / red `✕` pills, both `aria-label`'d WITH the part name (`Remove Helmet`, not `Remove` twelve times); edit mode = two steel `Input`s + cancel/save. CONTROLLED by the stage (SoulConstructor precedent) because analyse can replace the whole list underneath an open row. |
| Assets3D | `SpendConfirmModal` | The module's ONE blocking spend primitive: `Modal role="alertdialog" surface="steel"` restating the credit number in the body AND on the confirm pill, ghost cancel, confirm DISABLED while the price is `null`. Owns no copy — every string is passed in already localized, because extraction and meshing describe different sums. Callers close it FIRST, then mutate (§9). |
| Assets3D | `PartGenerationCard` | The shared paid plate for both grids (`kind: 'extract' \| 'mesh'` selects the locale namespace). RESOLVES the part's cited generation BY ID (`useLivePartGeneration`) — the aggregate embeds no `Generation`, so nothing else would populate the cache on a cold load. States: null citation → dim "not extracted yet" (a STATE, not a spinner) / resolving or processing → `animate-skeleton` square + `Progress` + amber caption / succeeded → poster in a SQUARE `Card surface="well" padding="none"` (`kind='mesh'` prints a green "meshed" instead — a `.glb` is not paintable by `<img>`) / failed → localized `errorCodeMessageKey` reason + green "credits refunded" `Badge`. An unreadable poll is AMBER `role="status"`, not red: nothing failed, we lost sight of it. A rejected MUTATION gets the reason and NO refund chip — nothing was charged. |
| Assets3D | `ExtractStage` | Paid grid (2/4 cols). Price on every button before the click; `null` price disables and pulses. **Single-part extract is click-to-spend** (owner decision 2026-07-20): it is the cheap, high-repetition step, and a modal per part would push users to batch blindly to escape the friction. The **extract-ALL batch IS gated** by `SpendConfirmModal` restating the total. One mutation call PER PART, so a rejection never aborts its siblings — failures are a `Map` keyed by part id, never one banner painting over eleven healthy plates. |
| Assets3D | `MeshStage` | Paid grid (2/3 cols) with a PER-PART tier `Select` (price carried in the row `meta` — choosing a tier is choosing a price, and hiding it until after the choice makes the picker a trap). **EVERY single mesh is gated by `SpendConfirmModal`** (owner decision 2026-07-20, mis-click parity: one careless tap costs more than a whole extraction pass) — the bare click only OPENS the dialog, which a test pins. A part with no extraction citation shows the honest reason (`mesh.blocked`), not a dead control; an empty catalog offers no tier and no spend at all. |
| Styles | `StyleLibrary` | The `/styles` body (§11): the 4 states over ONE `['styles']` list, split into «Мои стили» / «Встроенные» for display only. Tiles are square `well` plates (a style's sample is CONTENT, so it is recessed like a Gallery generation, never frosted); a builtin gets a `Badge` and no menu, a user style gets the ⋯ `Menu`. The `<li>` is `aria-labelledby` a deterministic `style-name-<id>` (not `useId` — it renders inside a map), so the row is named by the text it already shows. Deletion opens a steel `Modal` with a ghost cancel and a danger confirm. This component also OWNS the preview run, because it outlives the editor modal — a poll that died on close would strand a charged generation. |
| Styles | `StyleEditor` | The constructor, one `Modal` for create and edit (the `EntityEditor` law — identical fields, forking drifts): steel `Input` name, the positive fragment as a textarea carrying the MANDATORY sparkle, a negative textarea, and the advisory model `Select` built from the catalog the ROUTE injected. The sparkle's absolute placement lives on a WRAPPER div, never on `EnhanceButton`'s own `className` — that class lands on the component's `relative` box, which is the anchor its error/nudge chip hangs from (the `ImageNode`/`ShotInspector` precedent); the textarea gains `pr-12` so text never slides under the icon. The fragment field uses `htmlFor`+`id` rather than a wrapping `<label>`, or the button's text would fold into the textarea's accessible name. Edit mode adds a ghost «Сгенерировать превью · N кр» whose price comes from the model that will ACTUALLY run it (falling back to `flux-schnell` exactly as the submit does — a number that did not match the charge would be worse than none); it SAVES first, then hands the saved row up. Create mode has no preview button at all: a run cites a style by id, and one still being typed has none. |
| Styles | `StyleReferenceImages` | The package's picture half (ADR style-studio A4), a near-mirror of Cinema's `ShotReferenceImages` so the gesture is learned once: a focusable `role="group"` region that takes click (a `<label>` wrapping a sr-only input) / drop (`ring-portal` while dragging) / paste, all through the ONE shared `readImageFile` gate — a reject is a localized `role="alert"` and NO request. Thumbs are 64px `well` plates with an overhanging red ✕ (the destructive triad, outside the plate so it never covers the picture or shrinks below the hit floor); the in-flight upload is a `Skeleton` at the same radius. The `N / 3` counter turns AMBER at the cap and the add tile stays visible but `disabled` there, paired with an amber "all 3 used" line — the `PartsStage` law ("a disabled control with no reason reads as a bug"): keeping the tile keeps the CEILING legible, where a vanished control reads as the feature having moved. The drop/paste handlers refuse independently, so the disable is never merely visual. A permanent `text-[11px]` caption says the images are "applied where the model can use them": style refs are AMBIENT (silently dropped on a model with no reference support, trimmed before entity tags when the budget is full), so promising they reach the model would be a lie — a test pins the wording. |
| Soul | `SoulFacts` | Glass card: a `<dl>` on `divide-white/10` — mist-dim axis name (`soul.field.*`), white contract LABEL as the value — then the traits as amber `Badge`s and the notes verbatim. Labels, never model fragments: that is the payoff of storing the spec as structure. |
| Canvas | `NodeShell` | The chrome every node wears: a 288px OPAQUE steel `rounded-2xl` card with `shadow-glass`, the kind label left and the run status right. **PORTS ARE A 24px HIT AREA WEARING A 12px DOT (2026-08-03 fix):** the Handle itself is transparent and `size-6`, the visible dot is a `pointer-events-none` child. React Flow's 8px default was below pointer accuracy, and missing it does not do nothing — the press lands on the pane and the board PANS away, which is exactly what "привязка не работает" looked like. The editor pairs this with `connectionRadius={80}` for the drop side, and **a refused wire now SAYS why**: `canConnect` returns an `EdgeRefusal` CODE, `onConnectEnd` (the only hook that fires on a refused drop) raises one deduped `toast.info` with the localized `canvas.edge.refused.*` copy — a rule working as designed must never be indistinguishable from a broken feature. Status is the border AND the WORD together (`idle` white/10 · `processing` amber · `succeeded` green · `failed` red) — never color-only, and the word is the localized `canvas.status.*` key. React Flow `Handle`s are ports, not decoration: the left one exists only for kinds that accept input, the right only for kinds that produce it (video has none — a clip feeds nothing in the MVP). Handle colors need the `!` prefix to beat the vendor's own `.react-flow__handle` fill. |
| Canvas | `ImageNode` / `VideoNode` | The mini-composer, the 4-states rule inside a card: `Skeleton` while processing · `role="alert"` + AMBER ghost retry on failure (red is the failure STATUS, never the way out of it — the `ErrorState` law) · the media in a `rounded-lg` well plate on success · a quiet "not generated yet" plate at rest, which keeps the card's box so wires never jump. Then `VersionStrip`, the prompt textarea — which carries the `EnhanceButton` sparkle INSIDE the field (owner requirement 2026-07-30: every node prompt field has it; absolute placement on a WRAPPER, never through the component's own `className`, which is its `relative` anchor for the error/nudge chip — the `ShotInspector` precedent, plus `nodrag` so a pointer-down enhances instead of panning) — and the kit `Select` for model / aspect / duration — the app has exactly ONE dropdown (§6), and its rich rows (name · provider · price) are what make model choice legible at 288px. ONE price on the card, carried by the model control's `meta` and computed for the SELECTED duration, so a video node never advertises its cheapest clip. Every control carries `nodrag`, or React Flow pans the board instead of editing the field. TWO pills close the card (spec §3): the green `Generate` for this node, and a ghost `Run branch` for everything it depends on — both disabled while a queue owns this node, because a manual submit mid-branch is the double-charge the confirm dialog exists to prevent. A branch failure that never created a generation row (insufficient credits) speaks in a quiet AMBER line above the pills, since there is no error card for it to land in. With a CHARACTER wired in, the model picker narrows to reference-capable models (catalog `referenceMode`) — the `ModelSelect` law: filter the affordance, let the API decide — and a model chosen before the wire existed gets a quiet AMBER line (`canvas.node.characterModelHint`) plus a disabled Generate, because nothing has failed, the graph and the model merely disagree. |
| Canvas | `PromptNode` (shared prompt) | **2026-08-02, ADR `canvas-prompt-node`.** The board's reusable prompt: `NodeShell` at a permanent `idle`, OUTPUT port only, no model / price / history / Generate — the `EntityNode` posture, because the board must have ONE language for "furniture that feeds a producer". A 4-row textarea carrying the MANDATORY sparkle (absolute placement on a WRAPPER, `nodrag` on both — the `ShotInspector` precedent) over one mist-dim caption: `feeds N nodes`, or, at zero wires, what to DO instead («соедините с узлом изображения или видео») — "feeds 0 nodes" is a number pretending to be information, and the empty state of a card whose whole job is REACH has to name the next action. Its text merges into each wired child as `template ⏎ own` (never replaces — the point is one shared head plus a per-node variation), and the child restates that shared half above its own field, so the card cannot advertise one prompt and submit another. |
| Canvas | `UploadNode` / `NoteNode` | Upload = the same shell around a dashed `well` dropzone → the stored image; its status is derived (`uploadUrl ? succeeded : idle`), so a filled upload speaks the same green as a finished run. Note = the ONE node that skips `NodeShell`: an amber sticky (`specimen-amber/20` + `/40` border) with no ports and no status, narrower (224px) than a composer so it reads as annotation rather than competing with it. |
| Canvas | `RunBranchDialog` | The board's spend gate, and the only thing on the canvas allowed to INTERRUPT: one click can charge for several generations, so `alertdialog` (the `SpendConfirmModal` law — a credit is not undoable). An `<ol>` of steel rows, because the order is part of what is being confirmed (they run one after another and a failure stops the rest): ordinal · kind · model name · glow-green price, then a Total row, then quiet ghost Cancel + green confirm that RESTATES the number (`Run · 21 cr`). A row whose model is missing from the catalog prices as `—` and makes the total `—`, which DISABLES the confirm — a dialog is the last place to invent a price. A plan that cannot run shows its localized `canvas.runBranch.blocked.*` reason and NO run button: a disabled button with no explanation is a dead end. |
| Canvas | `EntityNode` (character) | The compact identity card, and the ONE generation-adjacent node that never runs: `NodeShell` at a permanent `idle` steel border, output port only, no prompt / model / price / history. Three elements, each stated exactly ONCE — the face on a 44px `well` plate (`object-cover`; a character with no photo keeps the plate's box and shows a quiet `☺`, so the row never changes height when a face lands), the name in the `Select` trigger, and the provenance as that `Select`'s caption (`canvas.node.fromSoul`). The avatar sits BESIDE the picker, not above it: a separate name line printed the same word twice inside 288px. The picker stays after a pick — recasting a chain is an edit, not a rebuild. Empty Soul library = the 4-states law inside a node: a `role="status"` hint plus a `nodrag` portal-tinted `Link` to `/soul`, never an empty dropdown. |
| Canvas | `NodePalette` | A 96px rail of kind buttons that are BOTH drag sources and click targets (six since 2026-08-02: `prompt`, glyph `¶`, joins between `character` and `note`) — drag-and-drop is not keyboard-reachable, so the click path is the accessible one, not a convenience. Glyphs are `aria-hidden`; the localized label is the accessible name. It lists only kinds that can actually be USED today — image · video · upload · character · note, ordered by the shape of a chain (media producers, then the subject, then the annotation). `upscale` / `remove-bg` exist in the contract and the edge rules already, but a button for a node that does nothing is a promise the product does not keep. |
| Shorts | `ShortsStudio` | The batch surface (§11 `/shorts`). Composition only: it owns the four states over `useTemplates()`, the picked format + tier + rows, the live price, and the gate. It REUSES `TemplateCard`, `TierPicker` and `useBalance` from Templates through that module's public API rather than re-drawing them — a shorts template IS an ordinary template, and a second card would be a second answer to "what does a template look like". The confirm handler CLOSES the dialog first and mutates after (§9): the thing worth watching is the board filling in, not a spinner on a question already answered. Creating the drafts charges ZERO and `['me']` is deliberately not refreshed there — refreshing the balance would make a free action look like it cost something. |
| Shorts | `VariantsTable` | One row per short, one column per template knob, with add / duplicate-in-place / remove. A REAL `<table>` with `scope="col"` headers: forty inputs in a flex grid are a grid to a sighted user and a flat list of forty unlabelled fields to everyone else — which is why each cell control passes `labelHidden` (§6) instead of dropping its label, and why the row ordinal rides in the duplicate/remove `aria-label`s (forty buttons all called "Duplicate" are forty identical announcements and one very expensive misclick). A table and not a "× 20" spinner because ADR §9 makes a batch VARY by default — YouTube demonetises repetitive mass-produced AI video, so the UI has to make varying the cheap gesture, and duplicate-then-edit is that gesture. Locked entirely while a run is in flight: the table describes a run already paid for. The CEILING is `TEMPLATE_BATCH_MAX_ROWS` (20) and it is enforced HERE, visibly — Add and Duplicate go disabled at the cap while Remove stays live (the ceiling is on growth, not on editing), an amber `N / 20` counter is always in the header, and a `role="status"` line states the reason: the API rejects a larger batch with a 400 that writes nothing, and losing a filled table of twenty-one hand-written hook lines is not an acceptable way to learn a limit (the `StyleReferenceImages` law — keep the control, disable it, say the number). Horizontal scroll lives on the table, never the page. CSV import is deferred. |
| Shorts | `RunBoard` | Per-film, per-beat status, and it owns NO STATE — it renders a derivation over the films plus the shared `['generation', id]` cache (ADR §2: no batch table, no job rows, no status machine). Each film is an `<article>` named after itself (forty landmarks a screen-reader user can move between) carrying a progress line, a portal link into `/cinema/` — a short IS a film, and that link is its exit — and a chip per beat. Chips are hairline pills using the closed triad and nothing else: mist-dim at rest/queued, AMBER rendering, GREEN ready, RED failed, and the WORD is always there beside the colour (never colour-only). A failed chip carries its own localized reason via `errorCodeMessageKey` (never a provider string) and its own priced ghost retry — a retry is a FRESH purchase, since the failed attempt was already refunded, so the number goes on the control and an unknown price disables it. Retry is also disabled while a batch runs: it would be a fifth submit past the concurrency cap, against a total confirmed for a different list of beats. Full 4 states. |

## 13. Header / toolbar patterns (the laws)

Added 2026-07-23 after an owner call that "паттерны хромают" — the first cinema-editor bar
read as a soup of controls. These are binding for ANY app/editor top bar or toolbar (AppShell,
CinemaEditorHeader, future workbench chrome). They are pattern rules, not new tokens.

1. **ZONES, one rhythm.** A bar is read left→right as ordered zones, not a flat row of equals:
   `[brand / back]` · `[context: title + meta]` · `flex spacer` · `[primary action + overflow]`
   · `│ divider │` · `[global chrome: balance · lang · account]`. One spacing rhythm inside a
   zone (`gap-2` between controls, `gap-3` between zones); never `gap-0/gap-1` crammed controls
   (min 8px between hit targets). A hairline `h-5 w-px bg-white/10` separates film/page actions
   from the quieter global chrome so the two never merge into one undifferentiated strip.

2. **One DOMINANT element per bar.** The screen's subject (the film title) is the visual anchor
   (`text-base`+ white); everything else is quieter (mist / mist-dim). Do NOT repeat the global
   wordmark on a focused editor screen — it competes with the subject and adds nothing a `‹`
   back link doesn't. The ONE primary action (the screen's main outcome — Export, Generate) is
   the single filled/tinted, icon-led button, placed FIRST in the action cluster so it reads
   first. Secondary/destructive actions go to a `⋯` overflow, never as loud siblings.

3. **CHIP-DROPDOWN vs SEGMENTED TOGGLE.** A **set-once / rarely-changed property** (canvas
   aspect, quality preset) is a **chip-dropdown** showing the current value with the selected
   option CHECKED in the popup — not a permanent row of toggles. Reserve **segmented toggles**
   (`PillGroup`, amber-selected) for **2–4 equal modes switched FREQUENTLY** (image/video type,
   gallery All/Images/Videos). Three permanent aspect toggles beside a title was the exact
   anti-pattern this rule retires: it shouted a decision the user makes once and buried the
   title. Mark the selected value — a dropdown that hides which option is active is a failed
   pattern (use a leading check + a same-size spacer on the rest to keep item alignment).

4. **META is context, not chrome.** Secondary facts about the subject (aspect · shot count ·
   duration) sit under/after the title as quiet `text-xs text-mist-dim` chips with `·` middots,
   never as buttons or loud pills. Numerals in meta use `tabular-nums`.

5. **Number-format law.** A large count NEVER renders raw (`1000000000`). Use locale-aware
   COMPACT notation (`Intl.NumberFormat(lng, { notation: 'compact', maximumFractionDigits: 1 })`
   → "1B" / "1 млрд") for the visible chip, and carry the EXACT grouped value in the
   `aria-label` + `title` (so precision is one hover / one screen-reader pass away). Keeps
   control widths stable and scannable. Applies to `BalanceChip` and any future count chip.

Recorded exceptions: `Modal` overlay is `role="presentation"` (2026-07-06 a11y fix);
failed cards and the submit banner may show the stored server/provider failure text
as a SECONDARY `text-mist-dim` caption under the localized primary (QA finding 3,
2026-07-07; never for `content_blocked`) — §9's "no raw server text" otherwise
stands; `auth.manifesto.*` (and the retired `landing.showcase.figure`/`items.*`,
plus `gallery.contentBlocked` — replaced by `errors.codes.contentBlocked`) keys
remain in BOTH locale files though no longer rendered — the keys-intact rule
outlives the components.
